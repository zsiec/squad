// Package tui hosts the bubbletea TUI for squad.
package tui

import "context"

// Run is the entry point invoked by cmd/squad/tui.go. Phase B Tasks 10+
// flesh this out into the actual program loop. For now it's a stub so the
// subcommand can register and Phase B can land incrementally.
func Run(ctx context.Context) error {
	return nil
}
