package bootstrap

import "context"

// Welcome runs the first-run flow: open the dashboard in the user's
// browser and write the ~/.squad/.welcomed sentinel so the open happens
// at most once. Skeleton stub — open + sentinel handling land in a
// follow-up.
func Welcome(ctx context.Context) error {
	return nil
}
